[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  # startCommand intentionally omitted — Dockerfile CMD handles port via Python os.environ
  healthcheckPath = "/healthz"
  healthcheckTimeout = 30
